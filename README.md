# 🚀 Система управления задачами - Docker версия

## 📋 Обзор
Полнофункциональная система управления задачами для команд с динамическими приоритетами и гибкой системой прав доступа.

## ✅ Основные возможности

### 🏗️ **Управление задачами**
- Создание задач с приоритетами 1-*
- Динамическое смещение приоритетов при завершении
- Назначение исполнителей
- Установка дедлайнов и статусов
- Фильтрация по командам, приоритетам, статусам

### 👥 **Командная работа**
- Создание команд пользователями
- Приглашение участников по никнеймам
- Администрирование команд
- Управление правами доступа (просмотр, редактирование, комментирование)

### 🔐 **Система прав доступа**
- **Администратор**: `view,edit,comment` - все права
- **Редактор**: `view,edit` - просмотр и редактирование
- **Комментатор**: `view,comment` - просмотр и комментирование
- **Наблюдатель**: `view` - только просмотр

### 📅 **Календарь**
- Визуальное представление задач по месяцам
- Отображение приоритетов (высокие задачи - толще)
- Фильтрация по командам
- Навигация между месяцами

### 🛠️ **Технологический стек**
- **Frontend**: Next.js 15, TypeScript, Tailwind CSS, shadcn/ui
- **Backend**: Next.js API Routes, Prisma ORM
- **База данных**: SQLite (разработка) / PostgreSQL (продакшен)
- **Runtime**: Node.js 20, Bun
- **Container**: Docker, Docker Compose

## 🐳 Быстрый старт

### 1. Клонирование репозитория
```bash
git clone <repository-url>
cd task-management-system
```

### 2. Запуск с Docker Compose
```bash
# Для локальной разработки (SQLite)
docker-compose up -d

# Для продакшена (PostgreSQL)
docker-compose -f docker-compose.prod.yml up -d
```

### 3. Проверка работоспособности
```bash
# Проверка API
curl http://localhost:3000/api/health

# Проверка веб-интерфейса
# Откройте http://localhost:3000 в браузере
```

## 📋 Конфигурация

### Переменные окружения
- `NODE_ENV`: `development` / `production`
- `DATABASE_URL`: `file:./dev.db` / `postgresql://...`
- `PORT`: `3000`

### Настройка портов
- **Frontend**: 3000
- **API**: 3000
- **Health Check**: 3000

## 🚀 Docker команды

### Управление контейнерами
```bash
# Запуск всех сервисов
docker-compose up -d

# Остановка
docker-compose down

# Перезапуск
docker-compose up -d --force-recreate

# Просмотр логов
docker-compose logs -f task-manager

# Выполнение команд в контейнере
docker-compose exec task-manager sh

# Мониторинг ресурсов
docker stats
```

## 📊 Доступные эндпоинты

### API
- `GET /api/health` - проверка состояния
- `GET /api/teams` - получение команд
- `GET /api/tasks` - получение задач
- `POST /api/auth/login` - аутентификация
- `POST /api/teams/[teamId]/members/[userId]` - управление правами

### Веб-интерфейс
- `http://localhost:3000` - основное приложение
- `http://localhost:3000/calendar` - календарь задач
- `http://localhost:3000/auth` - вход/регистрация

## 🔧 Разработка

### Локальная разработка
```bash
# Запуск в режиме разработки
docker-compose up -d

# Просмотр логов в реальном времени
docker-compose logs -f task-manager

# Перезапуск при изменениях
docker-compose up -d --force-recreate
```

### Продакшен
```bash
# Сборка продакшен образа
docker-compose -f docker-compose.prod.yml build

# Запуск продакшен версии
docker-compose -f docker-compose.prod.yml up -d
```

## 📈 Мониторинг

### Health Checks
```bash
# Проверка состояния всех сервисов
curl -f http://localhost:3000/api/health

# Мониторинг контейнеров
docker ps -f name="task-manager*"

# Проверка использования ресурсов
docker stats --no-stream
```

## 🐛 Масштабирование

### Горизонтальное масштабирование
```yaml
# docker-compose.scale.yml
version: '3.8'

services:
  task-manager:
    image: task-manager:latest
    replicas: 3
    environment:
      - DATABASE_URL=postgresql://postgres:5432/taskmanager
```

### Балансировка нагрузки
```yaml
# docker-compose.lb.yml
version: '3.8'

services:
  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
    depends_on:
      - task-manager-1
      - task-manager-2
      - task-manager-3
```

## 📝 Резервное копирование и восстановление

### Бэкап базы данных
```bash
# Создание бэкапа
docker exec task-manager sh -c "pg_dump taskmanager > backup_$(date +%Y%m%d).sql"

# Восстановление
docker exec -i postgres sh -c "psql -U taskuser -d taskmanager < backup_20241215.sql"
```

### Версионирование
```bash
# Создание тегов
docker tag task-manager:latest task-manager:v1.0.0

# Отправка в registry
docker push your-registry/task-manager:latest
```

---

## 🎯 Готовность к использованию!

Система полностью готова к развертыванию и эксплуатации в Docker-контейнерах. Все необходимые файлы созданы, конфигурации оптимизированы для разных сценариев использования."# dlmanager" 
