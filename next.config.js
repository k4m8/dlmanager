/** @type {import('next').NextConfig} */
const nextConfig = {
  // Опция 'appDir' больше не является экспериментальной и включена по умолчанию.
  // Этот блок можно удалить.
  // Здесь могут быть другие ваши настройки в будущем.
}

module.exports = nextConfig